# Render Blueprint for CmdClaw staging and production.
#
# Current mapping:
# - Render manages separate staging and production environments in one project.
# - Each environment owns its own web, worker, MCP gateway, Postgres, Key Value,
#   Zero cache, MinIO-backed S3, and observability stack (VictoriaMetrics,
#   VictoriaLogs, VictoriaTraces, Vector, Alertmanager, vmalert, and Grafana).
#
# Notes:
# - Keep secrets out of this blueprint and sync them from Infisical after the
#   resources exist. Use separate Render environment groups named `staging`
#   and `prod` so web, worker, and MCP can share secrets without staging and
#   production sharing credentials. Keep those groups managed outside this
#   blueprint; services reference them with `fromGroup`.
# - Render environment groups do not override service-level variables with the
#   same key, so any secrets provided by Infisical must not also be declared
#   directly on these services in this blueprint.
# - `APP_URL` and `NEXT_PUBLIC_APP_URL` should come from Infisical and point to
#   the correct Render hostname or custom domain for each environment before
#   testing auth/email flows.
# - The MinIO services are public because presigned URLs must be reachable from
#   the browser. If production traffic grows, prefer a managed object store such
#   as S3 or R2 and set `AWS_ENDPOINT_URL`/credentials from the production
#   environment group instead of running production object storage on Render.
# - Create the environment-specific document buckets once after the first deploy
#   (`cmdclaw-documents` for staging, `cmdclaw-documents-prod` for production).
# - The observability services stay on the private network. Operators reach the
#   Render private `10.0.0.0/8` range through the Tailscale subnet router defined
#   below. Grafana and the Victoria backends are exposed through one
#   Caddy-backed Tailscale ingress per environment. Web and worker export
#   telemetry to Vector via private-network host + port env vars.

previews:
  generation: "off"

projects:
  - name: cmdclaw
    environments:
      - name: staging
        databases:
          - name: cmdclaw-postgres-staging
            region: frankfurt
            plan: basic-256mb
            postgresMajorVersion: "18"
            ipAllowList: []

        services:
          - type: keyvalue
            name: cmdclaw-redis-staging
            region: frankfurt
            plan: starter
            ipAllowList: []

          - type: web
            name: cmdclaw-s3-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/minio
            dockerfilePath: docker/images/minio/Dockerfile
            healthCheckPath: /minio/health/live
            disk:
              name: cmdclaw-s3-data
              mountPath: /data
              sizeGB: 10
            envVars:
              - key: PORT
                value: "10000"
              - fromGroup: staging

          - type: worker
            name: cmdclaw-tailscale-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: docker/images/tailscale
            dockerfilePath: docker/images/tailscale/Dockerfile
            disk:
              name: cmdclaw-tailscale-state
              mountPath: /var/lib/tailscale
              sizeGB: 1
            envVars:
              - fromGroup: staging
              - key: TAILSCALE_VERSION
                value: 1.96.4
              - key: ADVERTISE_ROUTES
                value: 10.0.0.0/8

          - type: worker
            name: cmdclaw-postgres-tailscale-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: docker/images/tailscale
            dockerfilePath: docker/images/tailscale/Dockerfile
            disk:
              name: cmdclaw-postgres-tailscale-state
              mountPath: /var/lib/tailscale
              sizeGB: 1
            envVars:
              - fromGroup: staging
              - key: TAILSCALE_VERSION
                value: 1.96.4
              - key: TAILSCALE_HOSTNAME
                value: cmdclaw-postgres-tailscale-staging
              - key: TAILSCALE_TCP_FORWARD_TARGET_HOST
                fromDatabase:
                  name: cmdclaw-postgres-staging
                  property: host
              - key: TAILSCALE_TCP_FORWARD_PORTS
                value: "5432:5432"

          - type: pserv
            name: cmdclaw-zero-cache-staging
            runtime: image
            region: frankfurt
            plan: starter
            numInstances: 1
            image:
              url: rocicorp/zero:1.6.2
            disk:
              name: cmdclaw-zero-cache-data
              mountPath: /data
              sizeGB: 10
            envVars:
              - key: NODE_ENV
                value: production
              - key: ZERO_PORT
                value: "4848"
              - key: ZERO_UPSTREAM_DB
                fromDatabase:
                  name: cmdclaw-postgres-staging
                  property: connectionString
              - key: ZERO_CVR_DB
                fromDatabase:
                  name: cmdclaw-postgres-staging
                  property: connectionString
              - key: ZERO_CHANGE_DB
                fromDatabase:
                  name: cmdclaw-postgres-staging
                  property: connectionString
              - key: ZERO_REPLICA_FILE
                value: /data/replica.db
              - key: ZERO_ADMIN_PASSWORD
                generateValue: true
              - key: ZERO_QUERY_URL
                value: https://staging.cmdclaw.ai/api/zero/query
              - key: ZERO_QUERY_FORWARD_COOKIES
                value: "true"
              - key: ZERO_ENABLE_CRUD_MUTATIONS
                value: "false"
              - key: ZERO_LOG_FORMAT
                value: json

          - type: pserv
            name: cmdclaw-caddy-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/caddy
            dockerfilePath: docker/images/caddy/Dockerfile
            dockerCommand: caddy run --config /etc/caddy/Caddyfile --adapter caddyfile
            disk:
              name: cmdclaw-caddy-data
              mountPath: /data
              sizeGB: 1
            envVars:
              - key: PORT
                value: "10000"
              - fromGroup: staging
              - key: CMDCLAW_GRAFANA_DOMAIN
                value: grafana.ops.staging.cmdclaw.ai
              - key: CMDCLAW_VICTORIA_METRICS_DOMAIN
                value: victoria-metrics.ops.staging.cmdclaw.ai
              - key: CMDCLAW_VICTORIA_LOGS_DOMAIN
                value: victoria-logs.ops.staging.cmdclaw.ai
              - key: CMDCLAW_VICTORIA_TRACES_DOMAIN
                value: victoria-traces.ops.staging.cmdclaw.ai
              - key: CMDCLAW_GRAFANA_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-grafana-staging
                  property: host
              - key: CMDCLAW_VICTORIA_METRICS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-metrics-staging
                  property: host
              - key: CMDCLAW_VICTORIA_LOGS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-logs-staging
                  property: host
              - key: CMDCLAW_VICTORIA_TRACES_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-traces-staging
                  property: host

          - type: worker
            name: cmdclaw-caddy-tailscale-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: docker/images/tailscale
            dockerfilePath: docker/images/tailscale/Dockerfile
            disk:
              name: cmdclaw-caddy-tailscale-state
              mountPath: /var/lib/tailscale
              sizeGB: 1
            envVars:
              - fromGroup: staging
              - key: TAILSCALE_VERSION
                value: 1.96.4
              - key: TAILSCALE_HOSTNAME
                value: cmdclaw-caddy-tailscale-staging
              - key: TAILSCALE_TCP_FORWARD_TARGET_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-caddy-staging
                  property: host
              - key: TAILSCALE_TCP_FORWARD_PORTS
                value: "80:80,443:443"

          - type: pserv
            name: cmdclaw-victoria-metrics-staging
            runtime: image
            region: frankfurt
            plan: starter
            numInstances: 1
            image:
              url: victoriametrics/victoria-metrics:v1.140.0
            dockerCommand: /victoria-metrics-prod -storageDataPath=/victoria-metrics-data -vmalert.proxyURL=http://cmdclaw-vmalert-staging:8880
            disk:
              name: cmdclaw-victoria-metrics-data
              mountPath: /victoria-metrics-data
              sizeGB: 10

          - type: pserv
            name: cmdclaw-victoria-logs-staging
            runtime: image
            region: frankfurt
            plan: starter
            numInstances: 1
            image:
              url: victoriametrics/victoria-logs:v1.48.0
            dockerCommand: /victoria-logs-prod -storageDataPath=/victoria-logs-data
            disk:
              name: cmdclaw-victoria-logs-data
              mountPath: /victoria-logs-data
              sizeGB: 2

          - type: pserv
            name: cmdclaw-victoria-traces-staging
            runtime: image
            region: frankfurt
            plan: starter
            numInstances: 1
            image:
              url: victoriametrics/victoria-traces:v0.8.0
            dockerCommand: /victoria-traces-prod -storageDataPath=/victoria-traces-data
            disk:
              name: cmdclaw-victoria-traces-data
              mountPath: /victoria-traces-data
              sizeGB: 2

          - type: pserv
            name: cmdclaw-vector-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/vector
            dockerfilePath: docker/images/vector/Dockerfile
            envVars:
              - key: CMDCLAW_VICTORIA_METRICS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-metrics-staging
                  property: host
              - key: CMDCLAW_VICTORIA_LOGS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-logs-staging
                  property: host
              - key: CMDCLAW_VICTORIA_TRACES_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-traces-staging
                  property: host

          - type: pserv
            name: cmdclaw-alertmanager-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/alertmanager
            dockerfilePath: docker/images/alertmanager/Dockerfile
            disk:
              name: cmdclaw-alertmanager-data
              mountPath: /alertmanager
              sizeGB: 1
            envVars:
              - key: CMDCLAW_ALERT_ENV
                value: staging

          - type: pserv
            name: cmdclaw-vmalert-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/vmalert
            dockerfilePath: docker/images/vmalert/Dockerfile
            envVars:
              - key: CMDCLAW_ALERT_ENV
                value: staging
              - key: CMDCLAW_VICTORIA_METRICS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-metrics-staging
                  property: host
              - key: CMDCLAW_ALERTMANAGER_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-alertmanager-staging
                  property: host

          - type: pserv
            name: cmdclaw-grafana-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/grafana
            dockerfilePath: docker/images/grafana/Dockerfile
            disk:
              name: cmdclaw-grafana-data
              mountPath: /var/lib/grafana
              sizeGB: 2
            envVars:
              - key: GF_AUTH_ANONYMOUS_ENABLED
                value: "false"
              - key: GF_AUTH_DISABLE_LOGIN_FORM
                value: "false"
              - key: GF_SERVER_ROOT_URL
                value: https://grafana.ops.staging.cmdclaw.ai
              - key: GF_PLUGINS_PREINSTALL_SYNC
                value: victoriametrics-logs-datasource
              - key: CMDCLAW_VICTORIA_METRICS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-metrics-staging
                  property: host
              - key: CMDCLAW_VICTORIA_LOGS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-logs-staging
                  property: host
              - key: CMDCLAW_VICTORIA_TRACES_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-traces-staging
                  property: host
              - key: CMDCLAW_ALERTMANAGER_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-alertmanager-staging
                  property: host

          - type: web
            name: cmdclaw-web-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: .
            dockerfilePath: docker/images/web/Dockerfile
            dockerCommand: node .output/server/index.mjs
            healthCheckPath: /api/live
            envVars:
              - key: PORT
                value: "10000"
              - key: HOST
                value: 0.0.0.0
              - fromGroup: staging
              - key: DATABASE_URL
                fromDatabase:
                  name: cmdclaw-postgres-staging
                  property: connectionString
              - key: REDIS_URL
                fromService:
                  type: keyvalue
                  name: cmdclaw-redis-staging
                  property: connectionString
              - key: AWS_ENDPOINT_URL
                value: http://cmdclaw-s3-staging:10000
              - key: AWS_DEFAULT_REGION
                value: us-east-1
              - key: AWS_S3_BUCKET_NAME
                value: cmdclaw-documents
              - key: AWS_S3_FORCE_PATH_STYLE
                value: "true"
              - key: CMDCLAW_VECTOR_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-vector-staging
                  property: host
              - key: CMDCLAW_VECTOR_LOG_PORT
                value: "8686"
              - key: CMDCLAW_VECTOR_OTLP_HTTP_PORT
                value: "4318"
              - key: CMDCLAW_VECTOR_TRACES_PORT
                value: "5318"
              - key: CMDCLAW_MCP_BASE_URL
                fromService:
                  type: web
                  name: cmdclaw-mcp-staging
                  envVarKey: RENDER_EXTERNAL_URL
              - key: SANDBOX_AGENT_RUNTIME
                value: opencode
              - key: SANDBOX_DEFAULT
                value: daytona
              - key: E2B_DAYTONA_SANDBOX_NAME
                value: cmdclaw-agent-staging

          - type: web
            name: cmdclaw-mcp-staging
            runtime: docker
            region: frankfurt
            plan: standard
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: .
            dockerfilePath: docker/images/mcp/Dockerfile
            healthCheckPath: /
            envVars:
              - key: PORT
                value: "10000"
              - fromGroup: staging
              - key: AWS_ENDPOINT_URL
                value: http://cmdclaw-s3-staging:10000
              - key: AWS_DEFAULT_REGION
                value: us-east-1
              - key: AWS_S3_BUCKET_NAME
                value: cmdclaw-documents
              - key: AWS_S3_FORCE_PATH_STYLE
                value: "true"
              - key: MCP_GATEWAY_MANAGED_CHILDREN
                value: "true"
              - key: MCP_GATEWAY_CHILD_MODE
                value: start

          - type: worker
            name: cmdclaw-worker-staging
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: .
            dockerfilePath: docker/images/worker/Dockerfile
            dockerCommand: bun dist/index.js
            envVars:
              - fromGroup: staging
              - key: DATABASE_URL
                fromDatabase:
                  name: cmdclaw-postgres-staging
                  property: connectionString
              - key: REDIS_URL
                fromService:
                  type: keyvalue
                  name: cmdclaw-redis-staging
                  property: connectionString
              - key: AWS_ENDPOINT_URL
                value: http://cmdclaw-s3-staging:10000
              - key: AWS_DEFAULT_REGION
                value: us-east-1
              - key: AWS_S3_BUCKET_NAME
                value: cmdclaw-documents
              - key: AWS_S3_FORCE_PATH_STYLE
                value: "true"
              - key: CMDCLAW_VECTOR_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-vector-staging
                  property: host
              - key: CMDCLAW_VECTOR_LOG_PORT
                value: "8686"
              - key: CMDCLAW_VECTOR_OTLP_HTTP_PORT
                value: "4318"
              - key: CMDCLAW_VECTOR_TRACES_PORT
                value: "5318"
              - key: CMDCLAW_MCP_BASE_URL
                fromService:
                  type: web
                  name: cmdclaw-mcp-staging
                  envVarKey: RENDER_EXTERNAL_URL
              - key: SANDBOX_AGENT_RUNTIME
                value: opencode
              - key: SANDBOX_DEFAULT
                value: daytona
              - key: E2B_DAYTONA_SANDBOX_NAME
                value: cmdclaw-agent-staging

      - name: production
        databases:
          - name: cmdclaw-postgres-prod
            region: frankfurt
            plan: basic-1gb
            postgresMajorVersion: "18"
            ipAllowList: []

        services:
          - type: keyvalue
            name: cmdclaw-redis-prod
            region: frankfurt
            plan: starter
            ipAllowList: []

          - type: web
            name: cmdclaw-s3-prod
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/minio
            dockerfilePath: docker/images/minio/Dockerfile
            healthCheckPath: /minio/health/live
            disk:
              name: cmdclaw-s3-data-prod
              mountPath: /data
              sizeGB: 25
            envVars:
              - key: PORT
                value: "10000"
              - fromGroup: prod

          - type: worker
            name: cmdclaw-tailscale-prod
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: docker/images/tailscale
            dockerfilePath: docker/images/tailscale/Dockerfile
            disk:
              name: cmdclaw-tailscale-state-prod
              mountPath: /var/lib/tailscale
              sizeGB: 2
            envVars:
              - fromGroup: prod
              - key: TAILSCALE_VERSION
                value: 1.96.4
              - key: ADVERTISE_ROUTES
                value: 10.0.0.0/8

          - type: worker
            name: cmdclaw-postgres-tailscale-prod
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: docker/images/tailscale
            dockerfilePath: docker/images/tailscale/Dockerfile
            disk:
              name: cmdclaw-postgres-tailscale-state-prod
              mountPath: /var/lib/tailscale
              sizeGB: 1
            envVars:
              - fromGroup: prod
              - key: TAILSCALE_VERSION
                value: 1.96.4
              - key: TAILSCALE_HOSTNAME
                value: cmdclaw-postgres-tailscale-prod
              - key: TAILSCALE_TCP_FORWARD_TARGET_HOST
                fromDatabase:
                  name: cmdclaw-postgres-prod
                  property: host
              - key: TAILSCALE_TCP_FORWARD_PORTS
                value: "5432:5432"

          - type: pserv
            name: cmdclaw-zero-cache-prod
            runtime: image
            region: frankfurt
            plan: standard
            numInstances: 1
            image:
              url: rocicorp/zero:1.6.2
            disk:
              name: cmdclaw-zero-cache-data-prod
              mountPath: /data
              sizeGB: 10
            envVars:
              - key: NODE_ENV
                value: production
              - key: ZERO_PORT
                value: "4848"
              - key: ZERO_UPSTREAM_DB
                fromDatabase:
                  name: cmdclaw-postgres-prod
                  property: connectionString
              - key: ZERO_CVR_DB
                fromDatabase:
                  name: cmdclaw-postgres-prod
                  property: connectionString
              - key: ZERO_CHANGE_DB
                fromDatabase:
                  name: cmdclaw-postgres-prod
                  property: connectionString
              - key: ZERO_REPLICA_FILE
                value: /data/replica.db
              - key: ZERO_ADMIN_PASSWORD
                generateValue: true
              - key: ZERO_QUERY_URL
                value: https://cmdclaw.ai/api/zero/query
              - key: ZERO_QUERY_FORWARD_COOKIES
                value: "true"
              - key: ZERO_ENABLE_CRUD_MUTATIONS
                value: "false"
              - key: ZERO_LOG_FORMAT
                value: json

          - type: pserv
            name: cmdclaw-caddy-prod
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/caddy
            dockerfilePath: docker/images/caddy/Dockerfile
            dockerCommand: caddy run --config /etc/caddy/Caddyfile --adapter caddyfile
            disk:
              name: cmdclaw-caddy-data-prod
              mountPath: /data
              sizeGB: 2
            envVars:
              - key: PORT
                value: "10000"
              - fromGroup: prod
              - key: CMDCLAW_GRAFANA_DOMAIN
                value: grafana.ops.prod.cmdclaw.ai
              - key: CMDCLAW_VICTORIA_METRICS_DOMAIN
                value: victoria-metrics.ops.prod.cmdclaw.ai
              - key: CMDCLAW_VICTORIA_LOGS_DOMAIN
                value: victoria-logs.ops.prod.cmdclaw.ai
              - key: CMDCLAW_VICTORIA_TRACES_DOMAIN
                value: victoria-traces.ops.prod.cmdclaw.ai
              - key: CMDCLAW_GRAFANA_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-grafana-prod
                  property: host
              - key: CMDCLAW_VICTORIA_METRICS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-metrics-prod
                  property: host
              - key: CMDCLAW_VICTORIA_LOGS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-logs-prod
                  property: host
              - key: CMDCLAW_VICTORIA_TRACES_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-traces-prod
                  property: host

          - type: worker
            name: cmdclaw-caddy-tailscale-prod
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: docker/images/tailscale
            dockerfilePath: docker/images/tailscale/Dockerfile
            disk:
              name: cmdclaw-caddy-tailscale-state-prod
              mountPath: /var/lib/tailscale
              sizeGB: 2
            envVars:
              - fromGroup: prod
              - key: TAILSCALE_VERSION
                value: 1.96.4
              - key: TAILSCALE_HOSTNAME
                value: cmdclaw-caddy-tailscale-prod
              - key: TAILSCALE_TCP_FORWARD_TARGET_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-caddy-prod
                  property: host
              - key: TAILSCALE_TCP_FORWARD_PORTS
                value: "80:80,443:443"

          - type: pserv
            name: cmdclaw-victoria-metrics-prod
            runtime: image
            region: frankfurt
            plan: starter
            numInstances: 1
            image:
              url: victoriametrics/victoria-metrics:v1.140.0
            dockerCommand: /victoria-metrics-prod -storageDataPath=/victoria-metrics-data -vmalert.proxyURL=http://cmdclaw-vmalert-prod:8880
            disk:
              name: cmdclaw-victoria-metrics-data-prod
              mountPath: /victoria-metrics-data
              sizeGB: 10

          - type: pserv
            name: cmdclaw-victoria-logs-prod
            runtime: image
            region: frankfurt
            plan: starter
            numInstances: 1
            image:
              url: victoriametrics/victoria-logs:v1.48.0
            dockerCommand: /victoria-logs-prod -storageDataPath=/victoria-logs-data
            disk:
              name: cmdclaw-victoria-logs-data-prod
              mountPath: /victoria-logs-data
              sizeGB: 10

          - type: pserv
            name: cmdclaw-victoria-traces-prod
            runtime: image
            region: frankfurt
            plan: starter
            numInstances: 1
            image:
              url: victoriametrics/victoria-traces:v0.8.0
            dockerCommand: /victoria-traces-prod -storageDataPath=/victoria-traces-data
            disk:
              name: cmdclaw-victoria-traces-data-prod
              mountPath: /victoria-traces-data
              sizeGB: 10

          - type: pserv
            name: cmdclaw-vector-prod
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/vector
            dockerfilePath: docker/images/vector/Dockerfile
            envVars:
              - key: CMDCLAW_VICTORIA_METRICS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-metrics-prod
                  property: host
              - key: CMDCLAW_VICTORIA_LOGS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-logs-prod
                  property: host
              - key: CMDCLAW_VICTORIA_TRACES_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-traces-prod
                  property: host

          - type: pserv
            name: cmdclaw-alertmanager-prod
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/alertmanager
            dockerfilePath: docker/images/alertmanager/Dockerfile
            disk:
              name: cmdclaw-alertmanager-data-prod
              mountPath: /alertmanager
              sizeGB: 2
            envVars:
              - key: CMDCLAW_ALERT_ENV
                value: prod

          - type: pserv
            name: cmdclaw-vmalert-prod
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/vmalert
            dockerfilePath: docker/images/vmalert/Dockerfile
            envVars:
              - key: CMDCLAW_ALERT_ENV
                value: prod
              - key: CMDCLAW_VICTORIA_METRICS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-metrics-prod
                  property: host
              - key: CMDCLAW_ALERTMANAGER_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-alertmanager-prod
                  property: host

          - type: pserv
            name: cmdclaw-grafana-prod
            runtime: docker
            region: frankfurt
            plan: starter
            numInstances: 1
            dockerContext: docker/images/grafana
            dockerfilePath: docker/images/grafana/Dockerfile
            disk:
              name: cmdclaw-grafana-data-prod
              mountPath: /var/lib/grafana
              sizeGB: 10
            envVars:
              - key: GF_AUTH_ANONYMOUS_ENABLED
                value: "false"
              - key: GF_AUTH_DISABLE_LOGIN_FORM
                value: "false"
              - key: GF_SERVER_ROOT_URL
                value: https://grafana.ops.prod.cmdclaw.ai
              - key: GF_PLUGINS_PREINSTALL_SYNC
                value: victoriametrics-logs-datasource
              - key: CMDCLAW_VICTORIA_METRICS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-metrics-prod
                  property: host
              - key: CMDCLAW_VICTORIA_LOGS_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-logs-prod
                  property: host
              - key: CMDCLAW_VICTORIA_TRACES_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-victoria-traces-prod
                  property: host
              - key: CMDCLAW_ALERTMANAGER_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-alertmanager-prod
                  property: host

          - type: web
            name: cmdclaw-web-prod
            runtime: docker
            region: frankfurt
            plan: standard
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: .
            dockerfilePath: docker/images/web/Dockerfile
            dockerCommand: node .output/server/index.mjs
            healthCheckPath: /api/live
            envVars:
              - key: PORT
                value: "10000"
              - key: HOST
                value: 0.0.0.0
              - fromGroup: prod
              - key: DATABASE_URL
                fromDatabase:
                  name: cmdclaw-postgres-prod
                  property: connectionString
              - key: REDIS_URL
                fromService:
                  type: keyvalue
                  name: cmdclaw-redis-prod
                  property: connectionString
              - key: AWS_ENDPOINT_URL
                value: http://cmdclaw-s3-prod:10000
              - key: AWS_DEFAULT_REGION
                value: us-east-1
              - key: AWS_S3_BUCKET_NAME
                value: cmdclaw-documents-prod
              - key: AWS_S3_FORCE_PATH_STYLE
                value: "true"
              - key: CMDCLAW_VECTOR_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-vector-prod
                  property: host
              - key: CMDCLAW_VECTOR_LOG_PORT
                value: "8686"
              - key: CMDCLAW_VECTOR_OTLP_HTTP_PORT
                value: "4318"
              - key: CMDCLAW_VECTOR_TRACES_PORT
                value: "5318"
              - key: CMDCLAW_MCP_BASE_URL
                fromService:
                  type: web
                  name: cmdclaw-mcp-prod
                  envVarKey: RENDER_EXTERNAL_URL
              - key: SANDBOX_AGENT_RUNTIME
                value: opencode
              - key: SANDBOX_DEFAULT
                value: daytona
              - key: E2B_DAYTONA_SANDBOX_NAME
                value: cmdclaw-agent-prod

          - type: web
            name: cmdclaw-mcp-prod
            runtime: docker
            region: frankfurt
            plan: standard
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: .
            dockerfilePath: docker/images/mcp/Dockerfile
            healthCheckPath: /
            envVars:
              - key: PORT
                value: "10000"
              - fromGroup: prod
              - key: AWS_ENDPOINT_URL
                value: http://cmdclaw-s3-prod:10000
              - key: AWS_DEFAULT_REGION
                value: us-east-1
              - key: AWS_S3_BUCKET_NAME
                value: cmdclaw-documents-prod
              - key: AWS_S3_FORCE_PATH_STYLE
                value: "true"
              - key: MCP_GATEWAY_MANAGED_CHILDREN
                value: "true"
              - key: MCP_GATEWAY_CHILD_MODE
                value: start

          - type: worker
            name: cmdclaw-worker-prod
            runtime: docker
            region: frankfurt
            plan: standard
            numInstances: 1
            autoDeployTrigger: "off"
            dockerContext: .
            dockerfilePath: docker/images/worker/Dockerfile
            dockerCommand: bun dist/index.js
            envVars:
              - fromGroup: prod
              - key: DATABASE_URL
                fromDatabase:
                  name: cmdclaw-postgres-prod
                  property: connectionString
              - key: REDIS_URL
                fromService:
                  type: keyvalue
                  name: cmdclaw-redis-prod
                  property: connectionString
              - key: AWS_ENDPOINT_URL
                value: http://cmdclaw-s3-prod:10000
              - key: AWS_DEFAULT_REGION
                value: us-east-1
              - key: AWS_S3_BUCKET_NAME
                value: cmdclaw-documents-prod
              - key: AWS_S3_FORCE_PATH_STYLE
                value: "true"
              - key: CMDCLAW_VECTOR_HOST
                fromService:
                  type: pserv
                  name: cmdclaw-vector-prod
                  property: host
              - key: CMDCLAW_VECTOR_LOG_PORT
                value: "8686"
              - key: CMDCLAW_VECTOR_OTLP_HTTP_PORT
                value: "4318"
              - key: CMDCLAW_VECTOR_TRACES_PORT
                value: "5318"
              - key: CMDCLAW_MCP_BASE_URL
                fromService:
                  type: web
                  name: cmdclaw-mcp-prod
                  envVarKey: RENDER_EXTERNAL_URL
              - key: SANDBOX_AGENT_RUNTIME
                value: opencode
              - key: SANDBOX_DEFAULT
                value: daytona
              - key: E2B_DAYTONA_SANDBOX_NAME
                value: cmdclaw-agent-prod
