# Coworker Info Prototype

Question: should a coworker run detail page put the run identity at the top, keep the chat visible, and make `output.html` the primary middle surface?

This route is throwaway prototype code at `/prototype/coworker/info/<slug>`. In this prototype, `<slug>` is the coworker username. The page uses the latest run for that coworker.

Verdict: fill in after reviewing the route in the browser.
