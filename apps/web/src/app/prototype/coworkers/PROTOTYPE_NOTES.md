# User Coworker Prototype

Question: what should `/prototype/coworkers` optimize around when clicking a coworker should not open the editor, but should help the user understand and discuss work done by coworkers?

Variants:

- `profile`: coworker profile page. The coworker is the primary object; chat and runs are supporting sections.
- `runs`: run dashboard. Work is the primary object; coworkers are filters/owners.
- `hub`: conversation hub. Conversation is the primary object; run artifacts appear inline.
- `briefing`: manager briefing. The page answers what changed since the user last checked, grouped by coworker.

Verdict: pending user review.
