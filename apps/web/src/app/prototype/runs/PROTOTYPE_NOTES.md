# Coworker Runs Prototype

Question: what should `/prototype/runs` optimize for when showing every coworker run in one place?

Variants:

- `latest`: chronological ledger for answering what just happened.
- `coworker`: grouped table where each coworker is the primary unit.
- `attention`: review queue that starts with blocked and failed runs.
- `lanes`: board-style status lanes for quick operational scanning.

Verdict: pending user review.
