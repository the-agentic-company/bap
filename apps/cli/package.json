{
  "name": "@cmdclaw/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "cmdclaw": "src/bin/cmdclaw.ts"
  },
  "scripts": {
    "dev": "bun --watch --env-file=../../.env src/bin/cmdclaw.ts",
    "start": "bun --env-file=../../.env src/bin/cmdclaw.ts",
    "build": "bunx tsc --noEmit -p tsconfig.json",
    "check": "bunx tsc --noEmit -p tsconfig.json",
    "test": "bun run test:unit",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@cmdclaw/client": "workspace:*",
    "@cmdclaw/core": "workspace:*",
    "@cmdclaw/db": "workspace:*",
    "@stricli/core": "^1.2.6",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.0.18"
  }
}
