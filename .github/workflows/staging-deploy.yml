name: Staging Deploy

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch, tag, or commit SHA to prepare for staging"
        required: true
        default: "main"
        type: string

permissions:
  contents: read

concurrency:
  group: staging-deploy
  cancel-in-progress: false

jobs:
  resolve:
    runs-on: ubuntu-latest
    outputs:
      commit_sha: ${{ steps.resolve.outputs.commit_sha }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0

      - name: Resolve commit on main
        id: resolve
        env:
          REF: ${{ inputs.ref }}
        shell: bash
        run: |
          set -euo pipefail

          git fetch origin main --force

          commit_sha="$(git rev-parse "${REF}^{commit}")"
          git merge-base --is-ancestor "${commit_sha}" origin/main

          echo "commit_sha=${commit_sha}" >> "$GITHUB_OUTPUT"

  db-push:
    needs: resolve
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/db-push.yml
    with:
      ref: ${{ needs.resolve.outputs.commit_sha }}
      environment: staging
    secrets: inherit

  daytona-sandbox:
    needs: resolve
    uses: ./.github/workflows/daytona-sandbox.yml
    with:
      ref: ${{ needs.resolve.outputs.commit_sha }}
      environment: staging
    secrets: inherit
