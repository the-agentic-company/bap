name: Roll Back Render Services

on:
  workflow_call:
    inputs:
      environment:
        description: "Environment to target"
        required: true
        type: string
      web_deploy_id:
        description: "Render web deploy ID to roll back to"
        required: true
        type: string
      worker_deploy_id:
        description: "Render worker deploy ID to roll back to"
        required: true
        type: string
      mcp_deploy_id:
        description: "Optional Render MCP deploy ID to roll back to"
        required: false
        type: string
        default: ""
    secrets:
      RENDER_API_KEY:
        required: false

jobs:
  rollback:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    env:
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
      RENDER_WEB_SERVICE_NAME: bap-web-${{ inputs.environment }}
      RENDER_WORKER_SERVICE_NAME: bap-worker-${{ inputs.environment }}
      RENDER_MCP_SERVICE_NAME: bap-mcp-${{ inputs.environment }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Validate Render configuration
        env:
          ENVIRONMENT: ${{ inputs.environment }}
        shell: bash
        run: |
          set -euo pipefail

          missing=()
          [ -n "${RENDER_API_KEY}" ] || missing+=("RENDER_API_KEY")

          if [ "${#missing[@]}" -gt 0 ]; then
            printf 'Missing Render secret(s) for GitHub environment "%s": %s\n' \
              "${ENVIRONMENT}" "${missing[*]}" >&2
            printf 'Set these as secrets on the GitHub environment named "%s".\n' \
              "${ENVIRONMENT}" >&2
            exit 1
          fi

      - name: Roll back web
        env:
          WEB_DEPLOY_ID: ${{ inputs.web_deploy_id }}
        run: bun scripts/release/render-deploy.ts rollback --service-name "${RENDER_WEB_SERVICE_NAME}" --deploy-id "${WEB_DEPLOY_ID}"

      - name: Roll back worker
        env:
          WORKER_DEPLOY_ID: ${{ inputs.worker_deploy_id }}
        run: bun scripts/release/render-deploy.ts rollback --service-name "${RENDER_WORKER_SERVICE_NAME}" --deploy-id "${WORKER_DEPLOY_ID}"

      - name: Roll back MCP
        if: ${{ inputs.mcp_deploy_id != '' }}
        env:
          MCP_DEPLOY_ID: ${{ inputs.mcp_deploy_id }}
        run: bun scripts/release/render-deploy.ts rollback --service-name "${RENDER_MCP_SERVICE_NAME}" --deploy-id "${MCP_DEPLOY_ID}"
